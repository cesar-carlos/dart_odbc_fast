//! FFI unit tests (split from the former monolithic `ffi/tests.rs`).

mod support;

mod bulk;
mod catalog;
mod connection;
mod diagnostics;
mod errors;
mod init;
mod pool;
mod query;
mod stream;
mod transaction;
mod xa;
