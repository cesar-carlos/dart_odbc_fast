[workspace]
members = ["odbc_engine"]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2021"

# Release profile optimizations for workspace members
[profile.release]
lto = "thin"
codegen-units = 16
opt-level = 3
strip = true
